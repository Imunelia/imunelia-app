document.addEventListener('DOMContentLoaded',()=>{document.querySelectorAll('[data-study-form]').forEach(form=>{const status=form.querySelector('[data-form-status]');const collect=()=>{const data={form:form.dataset.studyForm,createdAt:new Date().toISOString(),fields:{},files:[]};new FormData(form).forEach((value,key)=>{if(value instanceof File){if(value.name)data.files.push({field:key,name:value.name,size:value.size,type:value.type});}else if(data.fields[key]!==undefined){data.fields[key]=[].concat(data.fields[key],value);}else data.fields[key]=value;});return data;};form.addEventListener('submit',e=>{e.preventDefault();const data=collect();const blob=new Blob([JSON.stringify(data,null,2)],{type:'application/json'});const a=document.createElement('a');a.href=URL.createObjectURL(blob);a.download=`${data.form}-${new Date().toISOString().slice(0,10)}.json`;a.click();URL.revokeObjectURL(a.href);if(status)status.textContent='Záznam byl exportován do zařízení. Soubory se zatím na server neodesílají.';});const print=form.querySelector('[data-print-form]');if(print)print.addEventListener('click',()=>window.print());});});